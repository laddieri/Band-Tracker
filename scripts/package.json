{
  "name": "band-tracker-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "One-off admin/migration scripts for Band Tracker (run locally, not deployed).",
  "type": "commonjs",
  "scripts": {
    "migrate": "node migrate-to-orgs.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
